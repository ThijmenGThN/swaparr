name: Cleanup untagged containers on GHCR

on:
  push:
    branches:
      - edge
    
jobs:
  clean:
    runs-on: ubuntu-latest
    steps:
      - uses: dataaxiom/ghcr-cleanup-action@v1
        with:
          token: ${{ secrets.GH_PAT }}
          dry-run: true
